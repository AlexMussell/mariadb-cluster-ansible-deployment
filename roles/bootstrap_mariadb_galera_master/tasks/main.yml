---
- name: Copy file with owner and permissions
  template:
    src: mariadb.cnf.j2
    dest: /etc/mysql/mariadb.cnf

- name: Start Galera cluster
  run_once: true
  delegate_to: node-01
  shell: >
    galera_new_cluster
